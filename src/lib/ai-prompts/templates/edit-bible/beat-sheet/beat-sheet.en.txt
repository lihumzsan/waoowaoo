# Role and Goal

Split the script text below into story beats. A beat is the smallest complete unit of story progress, usually one action, discovery, choice, relationship change, emotional turn, or scene progression.

Your job is only to split and describe these beats. Do not organize them into chapters, and do not design shots. Output JSON only.

# Fact Sources

- The script text below is the only fact source.
- Do not add plot, characters, places, motivations, or endings that are not in the text.

# Rules

- beats must be ordered by their appearance in the source text and must not overlap.
- Do not output character offsets, sourceStart, or sourceEnd. Output sourceAnchor only; the system computes exact offsets from block IDs and verbatim quotes.
- sourceAnchor.startBlockId/sourceAnchor.endBlockId must use block IDs from the input, such as `[p0001]`.
- sourceAnchor.startQuote/sourceAnchor.endQuote must be short verbatim source snippets inside those blocks. Do not paraphrase, summarize, or add ellipses. Prefer 6-30 characters/letters that are unique inside that block.
- Beat ranges should cover all story-bearing text. Skip only pure formatting, whitespace, or decorative text with no story function.
- Prefer natural story turning points: a new action starts, information is revealed, emotion shifts, a relationship changes, the scene changes, a goal changes, or risk escalates.
- Do not split mechanically by fixed character count, and do not pack a long story passage into one beat.
- Estimate `estimatedDurationSec` from the time this beat actually takes to perform, including dialogue, action, reactions, pauses, and transitions.
- Do not apply a fixed duration to every beat. A simple action may take only a few seconds; only complex story content needs more time.
- More beats do not make the complete script longer. The sum of all beat durations must stay close to the time needed to perform the complete script.
- If one beat clearly takes more than 120 seconds, split it at a natural story turn.
- Merge very short sentences without an independent story change into a neighboring beat.

# Input

Numbered script text blocks:
{{source_document}}

Script length (UTF-16 code units): {{source_length}}

# Output Schema

Return JSON only. Do not use markdown or explanations.

{
  "beats": [
    {
      "beatId": "beat_001",
      "title": "short title",
      "summary": "story progression or state change in this beat",
      "sourceAnchor": {
        "startBlockId": "p0001",
        "startQuote": "verbatim source snippet at the beat start",
        "endBlockId": "p0001",
        "endQuote": "verbatim source snippet at the beat end"
      },
      "estimatedDurationSec": 30
    }
  ]
}

# Self-check

- Are beats ordered by their source appearance and non-overlapping?
- Does every sourceAnchor use an input block ID, and does each quote appear verbatim inside that block?
- Did the beat ranges cover all story-bearing text?
- Did you split on natural story turns instead of fixed character counts?
- Does every estimatedDurationSec come from actual performance time, and does their sum fit the true scale of the complete script?
- Is the response strict JSON only?
