你负责分析场景图片的空间结构。请只根据输入的最终场景图片，输出这个场景真实的空间事实档案。

场景名称：{location_name}
原始场景描述（仅辅助理解，不得覆盖图片事实）：{location_description}

硬性规则：
1. 只描述图片里能看见、或能由画面结构直接推断的空间事实。
2. 不做导演决策：不输出镜头建议、机位建议或禁用区域。
3. 不输出任何坐标或网格类字段，包括 x/y、coordinate、blockedZones、cameraVantages、profileSource、placementZones。
4. 所有描述字段使用自然中文，只描述空间参照物（anchors）、参照物之间的相对关系、前景/中景/背景层次和光线方向。
5. anchors 至少 1 个；每个 anchor 都必须是图片中清楚可见、或由画面结构直接推断出的稳定空间参照物。
6. 只输出 JSON 对象，不要 Markdown，不要解释。

JSON 结构必须严格如下：
{
  "sceneSummary": "最终场景图里的真实空间结构总结",
  "anchors": [
    {
      "id": "anchor_left_door",
      "label": "左侧木门",
      "screenArea": "画面左后方",
      "depthLayer": "背景",
      "spatialRelations": ["木门右侧是长桌", "木门位于长廊尽头左侧"]
    }
  ],
  "depthLayout": {
    "foreground": "前景空间描述",
    "midground": "中景空间描述",
    "background": "背景空间描述"
  },
  "lightingDirection": "光线方向描述"
}
