Compress the supplied OpenAI Agents SDK transcript into one bounded continuation checkpoint that later turns can act on directly. Each line is one JSON AgentInputItem. Preserve the semantic relationship between user and assistant messages, function calls, relevant arguments, and matching results.

Rewrite the checkpoint from the supplied transcript. A previous context_checkpoint item, when present, is input material rather than immutable text: merge equivalent facts, remove duplication, replace superseded decisions, and keep the result bounded. Never append or reproduce an older checkpoint verbatim merely because it existed.

Keep only concrete facts needed to continue:
- The user's current goal, explicit preferences, constraints, and requested language
- Decisions already made, and the reasoning behind them
- Model-authored Choice options and what each stable option value means
- Tool calls that materially changed or retrieved state, including their relevant arguments and results
- Exact ids (resources, chapters, tasks, assets)
- Questions the user has not answered yet, and pending interactions
- Errors that occurred and why
- Work that finished, and work explicitly still outstanding

Merge repeated facts into one bullet. Omit pleasantries, transient progress narration, duplicated tool payloads, obsolete plans, and private reasoning. Never invent a fact that does not appear in the transcript. Return concise plain-text bullet lines with no headings or code fences.
Do not copy bulky `load_tools` parameter schemas into the checkpoint; retain only exact Operation ids that materially matter.

[transcript]
{transcript}
[/transcript]

Return exactly one rewritten checkpoint.
