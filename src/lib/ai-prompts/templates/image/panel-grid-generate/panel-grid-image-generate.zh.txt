你是一位专业的分镜画师。请根据以下分镜组数据生成一张 2x2 四宫格分镜图，用同一张图承载同一个视频 block 内的连续镜头。

【画面文字规则 - 只允许剧情内文字】
- 禁止把 cell 名称、panel id、角色名、场景名、图号、镜头编号、系统标签、注释、UI 或水印画进图像。
- 如果分镜组数据、镜头原文或用户提示明确要求书籍文字、屏幕文字、招牌、票据、片内字幕或其他场景世界内文字，必须按原文或剧情要求自然保留。
- 不要凭空添加随机文字、乱码、无关字幕或漂浮说明文字。

【四宫格布局 - 必须严格遵守】
- 输出必须是一张完整图片，内部为清晰的 2x2 四宫格
- cell_index 0 = 左上
- cell_index 1 = 右上
- cell_index 2 = 左下
- cell_index 3 = 右下
- 不得重排、合并、增删已有 cell
- 每个有效 cell 只画一个镜头画面
- 如果输入少于 4 个 cell，未使用的格子保持简单空白或低干扰背景，不得加入新剧情
- 四宫格之后会按固定 2x2 象限裁剪成单张分镜图，所以每个有效 cell 的主体必须完整落在对应象限内，不得跨格
- 可以有细分隔线或清晰边界，但不得出现 cell 编号、系统标签或说明文字

【画面比例】
整张四宫格图片的输出比例为：{aspect_ratio}
- 严格按照此比例输出整张图
- 每个 cell 会按固定象限裁剪，不要让关键主体贴近象限边界

【连续性目标】
- 四宫格内所有镜头属于同一个视频 block，应天然保持角色身份、服装、场景空间、光照方向、色彩风格、镜头轴线和运动逻辑一致
- 每个 cell 按自己的镜头描述、shot_blocking、摄影规则执行
- 相邻 cell 之间应体现合理的动作和空间连续性，但不得改变任何镜头本身的叙事要求

【参考图使用规则】
- 角色参考：用于锁定角色身份、脸型、发型、服装、体型和主要外观特征
- 场景参考：用于参考空间布局、关键锚点、入口出口、家具/物体相对位置
- context.reference_images 是本次实际上传参考图的顺序映射，必须按其中的 image_no 识别参考图身份
- 不要读取图片内文字、黑边、标签或水印来判断身份
- 参考图中的旧美术风格不得覆盖本次项目视觉风格，最终画风以【风格要求】为准
- 每个 cell 的 panel_context 中如包含 location_reference.spatial_profile，必须作为场景空间事实依据
- 每个 cell 的 panel.shot_blocking 必须作为人物站位、相对位置、视线、机位和构图的最高优先级指令

【分镜组数据】
{storyboard_grid_json_input}

【镜头原文】
{source_text}

【风格要求】
画面风格：{style}
- 四个 cell 必须共享同一项目视觉风格
- 角色参考图只用于身份和主要外观一致，不用于覆盖项目画风
- 场景参考图只用于空间布局和关键锚点一致，不用于覆盖项目画风

【最终规则】
- 严格按 cell 顺序生成，不得重排
- 每个有效 cell 都必须能单独裁切成完整分镜
- 禁止生成单张大场景替代四宫格
- 禁止生成超过一个四宫格
