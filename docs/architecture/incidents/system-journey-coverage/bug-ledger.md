# Discovery bug ledger

This ledger records observed failures, not speculative defects. A row remains
open until the original production or harness path is re-executed successfully.

| ID | Journey / checkpoint | Observed failure | Evidence | Classification | Downstream impact | Status |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-GH-001 | Golden environment startup before `GJ-MAIN-STORY-TO-FINAL-DELIVERABLE` | Starting Golden from a second worktree ran global `docker compose ... down -v --remove-orphans`, removed the development `waoowaoo-mysql`, `waoowaoo-redis`, and `waoowaoo-minio` containers, then failed because fixed `waoowaoo-test-mysql` already belonged to another run. After initial isolation, a failed Playwright run left its scoped containers alive because webServer termination did not reliably execute signal cleanup; Vitest also had a second direct global teardown bypass. | Red: initial `npm run test:golden:mainline` and first critical Workflow Lab run. Green: `npm run test:golden:self` started two independent MySQL/Redis scopes, stopped A, and proved B healthy; a failing real browser mainline used dynamic database port `55008` and removed only its Golden scope; a real Task integration ran 10/10 on dynamic port `55021` and removed only `waoowaoo-test-7803ab0656`. Docker before/after retained the same development and pre-existing test container IDs | Harness ownership / isolation, P0 for concurrent Goals | Removed: each worktree/run now owns its Compose project, dynamic endpoints, scoped cleanup, Playwright global teardown, and the Vitest teardown bypass | closed |
| BUG-GH-002 | Second Golden invocation immediately after a failed downstream run | The previous run removed its scoped MySQL/Redis while a fixed-port health coordinator and Next/worker descendants were still reachable. The next run observed the stale `3199` health endpoint before its own environment finished, reused an old `environment.json`, and began requests while the newly created database was being torn down. Docker Desktop then exposed a second harness flaw: newly published all-interface dynamic ports accepted TCP without delivering the MySQL/Redis protocol. | Red: consecutive runs showed stale scope/health reuse and `Server shutdown in progress`. Control: a disposable Redis bound to `127.0.0.1:0` returned `PONG` while the all-interface bind did not. Green: repeated focused and full product runs used distinct runtime IDs and dynamic endpoints, fully started MySQL/Redis/app/worker, then removed only their own scope with no `waoowaoo-golden-*` survivor. | Harness runtime identity, loopback transport and teardown ordering; recurrence of partial isolation from BUG-GH-001 | One inherited runtime identity supplies dynamic app/coordinator ports; Compose publishes MySQL/Redis only on `127.0.0.1`; application and worker receive the exact scoped endpoints; startup failures clean the predeclared scope; coordinated teardown terminates the process group before removing its data scope. | closed |
| BUG-PROD-001 | `GJ-I18N-CRITICAL-PROJECT` on the English product surface | The language switcher's accessible name was generated from the target locale, so an English page visually displayed English while assistive/browser role lookup exposed Chinese. | Fail-before: the real English UI could not locate the switch-language button by its current-language contract. Green: the same Journey creates one project in English, switches through the real confirmation, observes Chinese UI, and deletes the same durable project identity. | Product shell locale ownership | `LanguageSwitcher` now derives the trigger's visible/accessibility copy from the current locale; only the selected target owns confirmation and subsequent navigation. | closed |
| BUG-PROD-002 | `GJ-AUTH-SESSION-RECOVERY` real Profile logout | The original client logout could lose its CSRF request during competing navigation. The first diagnostic replacement manually posted the NextAuth protocol and cleared the server cookie, but left the SessionProvider cache authenticated, proving a second client session writer. | Fail-before 1: `/api/auth/signout` submitted an undefined CSRF token. Fail-before 2: signout returned 200 and `/api/auth/session` was `{}`, while Navbar still displayed the user. Green: the Journey waits for authenticated UI, signs out once, observes unauthenticated Navbar plus empty server session, rejects wrong credentials, then restores the same durable user. | Product shell session lifecycle / duplicate client writer | Profile keeps the navigation-race fence but delegates CSRF, cookie removal and client cache broadcast to NextAuth `signOut({redirect:false})`, then performs locale navigation. Hand-written protocol parsing was deleted. | closed |
| BUG-PROD-003 | Project creation from a fresh user's workspace | Successful create navigated to Profile for required model setup while also launching a project-list refetch owned by the page being left; navigation aborted that request and produced a false product error. | Fail-before browser trace: committed project followed by an aborted `/api/projects` refresh during Profile navigation. Green: CRUD and response-loss Journeys create exactly one durable project with clean browser observations. | Product shell action successor ownership | The create result now chooses one successor: navigate to setup, or remain and refetch. It never starts both. | closed |
| BUG-PROD-004 | `GJ-ASSET-HUB-PROJECT-REUSE` first navigation | `useSSE` read `window.sessionStorage` during the client component's server render, so the first Asset Hub document returned 500 `ReferenceError: window is not defined`; client recovery later disguised the failure. | Fail-before 500 response body and stack identify `readStoredCursor → useSSE → AssetHubPage`. Green: focused real Chromium Journey receives a clean initial document, creates/imports through both real UIs, reloads, and verifies durable source/owner/appearance identity. | Async transport cursor capability boundary | The single cursor reader returns the empty transport cursor during server render and reads durable session storage only when the browser capability exists. | closed |
| BUG-PROD-005 | `GJ-PROJECT-CROSS-USER-ISOLATION` direct foreign-project URL | After the authoritative project read returned 403, the page interpreted `episodes=[]` as a legitimate zero state and still POSTed an automatic first episode. The second 403 surfaced as `pageError: Forbidden`. | Fail-before trace: project data 403, then `/episodes` POST and `handleCreateEpisode` page error. Green: the four-scenario auth/project/permission run passes; the foreign project remains unchanged and no setup write follows denial. | Project initialization precondition / read-before-write authority | Automatic zero-state initialization is admitted only after a successful authoritative Project object exists and no project error is present. | closed |
| BUG-ASST-EXT-001 | `GJ-MAIN-STORY-TO-FINAL-DELIVERABLE` after the durable `script_ready_for_review` boundary | The real browser registered a user, created a project, completed intake, generated a source script, reloaded at the review boundary, and then reached `interaction_failure` after confirming the script. Durable stage history stopped at `ready_to_ingest_script → script_ready_for_review`. | Playwright trace, screenshot, video, browser observations, app/worker logs, and attached read-only oracle from the 2026-07-12 discovery run | Assistant lifecycle failure; formal repair commits are now integrated on the target branch | The original mainline remains the acceptance path; no fix is considered closed until it passes without a bypass. | repair integrated; revalidation pending |
| BUG-ASST-EXT-002 | `GJ-DOWNSTREAM-CHECKPOINT-TO-FINAL-DELIVERABLE` at `bible_generating` | After a production Workflow Lab fork resumed through the real script-review UI and submitted the real bible-generation task, the workspace rendered `Maximum update depth exceeded`. | Playwright trace, screenshot, video, browser observations, app/worker logs, and attached read-only oracle from the 2026-07-12 downstream run | Canvas render feedback loop; the one-way projection/layout/measurement repair is now integrated on the target branch | The actual controlled ReactFlow Canvas must remain mounted during revalidation; omitting it cannot count as success. | repair integrated; revalidation pending |
| BUG-ASST-EXT-003 | Billable style generation Approval after the real `bible_review` Choice | The Approval card survived reload and the server accepted `approved: true`. Resume started `generate_edit_style_previews`, then rejected its own legal resumed invocation with `OPERATION_PLAN_INPUT_CHANGED: the operation input differs from the approved immutable plan`; no image task was submitted and the Run ended `PROJECT_AGENT_WORKFLOW_CONTINUATION_MISSING`. | Real Chromium trace request/stream, screenshot and video. Stream order: interruption resolved → operation activity started → activity failed → immutable-plan conflict → Run failed. This supplies the missing exact reproduction for the earlier audit's “Approval snapshot already has a defense; re-locate the symptom” finding. | Assistant Approval / planned invocation reconstruction; shared nullable-input normalization and stable semantic hashing are integrated | All input-hash bypass code is removed. The original Approval Journey must prove that the frozen snapshot and resumed input are semantically identical. | repair integrated; revalidation pending |
| BUG-ASST-EXT-004 | Approved `generate_edit_style_previews` batch commit after temporarily passing BUG-ASST-EXT-003 | The operation has three planned image tasks but concurrently calls `submitPlannedOperationTask` once per task. That API delegates to `submitApprovedOperationPlanTasks`, which submits the entire approved batch every time. The three same-transaction submissions race; a broad P2002 handler observes a just-created Task before its Created event and reports `TASK_BATCH_CREATED_EVENT_MISSING`. | Real Approval-response stream after the narrow input-hash diagnostic: operation activity starts, then fails with `TASK_BATCH_CREATED_EVENT_MISSING:opt_*`; code path proves per-task `Promise.all` → whole-batch submitter. | Approved-plan batch authority remains fragmented | The serialization bypass is removed. Permanent repair must submit the approved batch exactly once and return the result map. | open; architecture repair required |
| BUG-ASST-EXT-005 | Legal Task completion follow-up after style generation and after a failed chapter render | The task continuation sometimes settles into `awaiting_task` and then the runtime reports `PROJECT_AGENT_TASK_FOLLOW_UP_SETTLEMENT_MISSING`; its error path attempts the illegal transition `awaiting_task → failed`, which masks the first failure. | Repeated real app/worker logs across independent Journey runs; the same pair appears after both successful media batches and the later FFmpeg render failure. | Assistant execution-handoff settlement | Terminal Run immutability is integrated, so a later failure cannot replace the primary error; the missing Task follow-up settlement itself still requires repair and real-chain revalidation. | partially repaired; follow-up settlement open |
| BUG-ASST-EXT-006 | Task terminal Wait and paid Approval consumption after legal Run events | Waits and Approval interruptions retain their creation-time `runVersion/eventSeq`. The Run legally advances when it enters the waiting status or when another task in the same batch completes, then the user response or next task terminal is rejected as `PROJECT_AGENT_RUN_EVENT_STALE`. Three concurrent style images, a single storyboard task and the storyboard-image Approval all reproduced the same defect. | Exact database rows and errors show stored fence `18/106` versus current Run `19/107`; without a diagnostic the reconciler repeats forever. With a raw locked-current-fence diagnostic the unchanged real UI action and task terminals proceed. | Interruption consumption now locks the current Run fence and starts a response segment; Task Wait consumption still requires formal convergence | Both refresh-fence diagnostics are removed. Choice/Approval and multi-Task Wait paths must be re-run independently. | partially repaired; Task Wait open |
| BUG-ASST-EXT-007 | Session-state read while a completed chapter Task hands off to audio-layer planning | The production session snapshot reads an event watermark, builds a multi-table state, then reads the watermark again. Two immediate retries both observed legal concurrent events and the route converted that ordinary busy interval into non-retryable HTTP 500 `PROJECT_AGENT_SESSION_SNAPSHOT_UNSTABLE`. The real page recovery/test observer therefore failed even though the chapter render had completed and AI was progressing to soundscape planning. | Exact app log at `2026-07-12T09:07:15.553+08:00`, request/response in Playwright trace, and source contract showing only two optimistic attempts before throwing. | Assistant session read-consistency protocol | The retry-ignore diagnostic is removed. Session snapshot convergence requires an architecture repair and the original high-churn handoff re-run. | open; architecture repair required |
| BUG-WL-001 | `GJ-STAGE-SCRIPT-READY-FOR-REVIEW` canonical checkpoint | Workflow Lab first failed to list the checkpoint because it searched historical `data-assistant-choice-card` message parts instead of the durable Choice interruption. After switching identity authority, fork reached the transaction but failed `PROJECT_AGENT_CHOICE_REVIEWED_RESOURCE_MISSING` because `script_ready_for_review` did not clone the source document/EditBible reviewed by that Choice; the first clone attempt then normalized it to the later `bible_ready_for_review` stage. | Red matrix: 12 `GOLDEN_STAGE_CHECKPOINT_MISSING` failures. Intermediate red: POST `/workflow-lab` 400, then target stage mismatch. Green: focused real Chromium run kept the original mainline red but passed `GJ-STAGE-SCRIPT-READY-FOR-REVIEW`, including durable checkpoint discovery, production fork, correct target stage, UI submission, and advance to `ready_to_generate_bible`. | D recurrence: Workflow Lab had repeated checkpoint recovery fixes, but new lifecycle stages required hand-updating separate message and clone interpretations | Choice identity now comes from durable interruption; script-review clones exactly its source document/EditBible and preserves its stage; old message-card integration simulations were deleted | closed |
| BUG-GP-001 | `GJ-DOWNSTREAM-CHECKPOINT-TO-FINAL-DELIVERABLE` bible generation | The local external-model simulator returned generic prose for the four prompt-only edit-bible extraction calls, so the real worker correctly failed JSON parsing. | Red durable diagnostics: `EDIT_BIBLE_EXTRACTION_FAILED / PARSE_ERROR`. Green: provider self-test validates one cross-consistent bundle against production normalizers; real Chromium rerun completed `edit_bible_generate` and persisted `ready_for_review` with one chapter. | Golden external-boundary fixture gap, not product behavior | The local provider now implements the four real prompt contracts while the production worker, parsers, persistence, queue, and database remain real | closed |
| BUG-GP-002 | `GJ-DOWNSTREAM-CHECKPOINT-TO-FINAL-DELIVERABLE` at `ready_to_generate_style_previews` | The local model simulator routed by broad text fragments across the whole prompt history. First, an AI command containing historical Bible instructions returned an old Bible JSON object instead of calling the currently allowed `generate_edit_style_previews` tool. After tools were given precedence, the operation's own prompt-only `style-preview-options` call still embedded `voiceProfile/worldRules`, so the simulator again returned the Bible schema. | App logs distinguish both attempts. Green reruns executed the real style Approval, three image Tasks and the durable visual-style Choice through fresh Chromium contexts. | Golden external-boundary fixture routing gap, not product behavior | Tool-bearing requests use the current production toolset; prompt-only style option requests use a specific contract validated by the production schema. | closed |
| BUG-WL-002 | `GJ-DOWNSTREAM-CHECKPOINT-TO-FINAL-DELIVERABLE` from the canonical `bible_ready_for_review` checkpoint | Workflow Lab cloned the durable Bible but silently omitted its chapter plans, then forced later artifacts onto chapter zero. | Red Playwright staircase rejected `confirm_bible` with `EDIT_BIBLE_CONFIRMATION_REQUIRES_CHAPTERS`. Green staircase repeatedly resumed Bible, style and asset checkpoints, retained the cloned chapter identity through shot planning, storyboard and video planning, and reached the video Approval. | Workflow Lab checkpoint fidelity / canonical identity loss | Chapters and their source/provenance identity are cloned before downstream artifacts; edit scripts, storyboards and video groups consume explicit ID maps. | closed |
| BUG-WL-003 | Repeated downstream checkpoint forks | Each fork prefixed the full previous LAB project name, eventually exceeding the database column and failing with Prisma `P2000`. | Repeated real staircase runs failed only after nested forks. Focused naming self-test and subsequent multi-fork browser runs keep every generated name within the shared project-name contract. | Workflow Lab derived identity / project validation | One shared bounded naming function preserves the stage and a useful source suffix without recursively copying unbounded LAB prefixes. | closed |
| BUG-WL-004 | `ready_to_generate_videos` Approval checkpoint | Workflow Lab could list a durable Approval via the tool call but restored only Choice checkpoints. The cloned episode had the correct business stage but `currentRun=null` and `pendingInteraction=null`, so there was no real Approval to submit. | Fail-before session-state from the cloned project showed `ready_to_generate_videos` with no Run. Green discovery rerun cloned the frozen operation plan with rewritten project/episode/target identities, restored an `awaiting_approval` Run, reloaded the real card, consumed it once and submitted real video Tasks. | Workflow Lab Approval recovery / frozen plan identity | Approval checkpoints now originate from the durable interruption even when no historical display part exists; clone restores Run, interruption, serialized RunState and a newly hashed, unexpired target-scoped plan snapshot. | closed |
| BUG-WL-005 | Full canonical stage matrix listed Approval checkpoints that could never be forked | Tool-call identity was stored in a last-write-wins map, so duplicate historical tool calls could bind the wrong interruption. The list also advertised Approval rows whose durable `runState` was missing; the POST then inevitably failed `WORKFLOW_LAB_APPROVAL_RUN_STATE_MISSING`. A restored pending Approval without a retained message part was invisible, making a checkpoint usable only once. | First matrix: 19 executed, 9 passed / 10 failed. Second matrix: the three affected Approval stages returned HTTP 400 with the exact missing-run-state code. Logic fail-before proves ambiguous duplicate tool calls. Green: pending durable Approval is independently listed, consumed orphan rows are excluded, missing runState is fail-closed, exact interruptionId outranks the unique tool-call fallback, and storyboard/video Approval checkpoints can be forked again and consumed through real Chromium. | Workflow Lab checkpoint authority and executable capability | Pending durable interruption identity is authoritative. Tool-call fallback is admitted only when unique; an Approval must carry frozen runState before listing; pending durable interruptions remain discoverable without a historical UI part. | closed |
| BUG-WL-006 | Reusable asset/storyboard/video Approval checkpoints corrupted later-stage facts or collided on the second fork | Historical stage entries were cloned from a source project that had since advanced, so early Bible clones retained final Bible JSON and later clones projected `completed` or `ready_to_generate_videos`. Re-forked video plans reused plan-only target IDs and hit a primary-key conflict. Asset plans reserved both parent and child identities, but only Task target IDs were rewritten; the clone also cleared an existing requirement target required by the frozen plan's stale-value fence. | Full matrix: 20 executed, 14 passed / 6 failed. Exact reds included `Confirmed edit bible cannot be revised`, target stage `completed` instead of `ready_to_render_final`, video `PRIMARY` conflict, and asset `OPERATION_PLAN_STALE` expected non-null target versus observed null. Green: all 11 admitted stable checkpoints have real browser evidence; repeated storyboard-image and video Approval forks passed in 7.1s/6.5s, and the unchanged asset probe passed 6/6 with its target case in 6.4s. | Workflow Lab as-of source ownership, immutable plan identity and asset association fidelity | The harness preserves the first real source per stage and freezes active Approval sources before continuing. OperationPlan now explicitly declares `reservedIdentityIds`; clone rewrites those plus Task targets through one replacement map. Asset Approval clones preserve mapped existing requirement targets. Internal transient stages are covered by the longest Journey but are not advertised as reusable checkpoints. | closed |
| BUG-ASSET-001 | `GJ-ASSET-HUB-CROSS-PROJECT-DENIAL` | User B authenticated through project B, supplied project A's character ID to the real copy route, received HTTP 200, and replaced A's appearance/source association with B's global character. | Fail-before: isolated Chromium + production route/service/MySQL returned 200 at the expected closed boundary; trace, screenshot and video are retained in the run artifacts. Git history traces the unscoped target lookup to the unified asset service introduction. | Asset scope/owner authority missing across multiple mutation paths; class D | Add one service-level owner/project/kind/parent-child resolver, make copy atomic, remove raw-ID mutation authority, and rerun the unchanged two-user Journey plus ordinary reuse. | open |
| BUG-PROD-005 | `GJ-ASSET-HUB-CROSS-PROJECT-DENIAL` cleanup followed immediately by logout | Project DELETE returned 200, then its handler launched `fetchProjects` without awaiting it. Immediate logout/navigation aborted the page-owned request and logged `Failed to fetch`. | First repaired ownership rerun: the foreign write was closed and its durable target unchanged, but clean-browser oracle failed on the exact post-delete request race. `git blame` traces the unawaited sibling branch to the initial workspace implementation. | Recurrence of product-shell action successor ownership (PS-06) | The stay-on-page delete action awaits its list refresh before completing; no timer, suppression, or allowed-error exception is added. | open |
| BUG-GP-003 | First core edit plan after style Choice | The local provider sent `chapterId:null` literally into a production prompt contract that expects the system to resolve the active chapter identity. | Fail-before production parser/operation rejection; green provider self-test plus repeated real edit-script Task completion. | Golden external-boundary fixture gap | The simulator treats null as a system-resolved placeholder and returns data keyed to the real prompt facts; production resolution remains unchanged. | closed |
| BUG-GP-004 | Core edit-plan structured output | Generic JSON did not satisfy the production shot/generation-segment schema. | Production parser self-test and real `edit_script_generate` worker completion. | Golden external-boundary fixture gap | A dedicated prompt contract returns cross-consistent shots and continuous generation segments. | closed |
| BUG-GP-005 | Location candidate generation | The generic provider response did not satisfy the location candidate schema used during required-asset generation. | Production schema self-test and real location Task completion. | Golden external-boundary fixture gap | Dedicated location-candidate output is validated by the same production parser. | closed |
| BUG-GP-006 | Location spatial-profile vision call | The model simulator lacked the structured vision response required to materialize location spatial facts. | Production parser self-test and real location Task completion with ready spatial profile. | Golden external-boundary fixture gap | Dedicated spatial-profile response preserves the real vision request and production parser. | closed |
| BUG-GP-007 | Shot execution planning | Generic output could not satisfy per-shot camera/blocking identities and continuous-video segments. | Production parser self-test and repeated real `edit_shot_execution_plan_generate` Task completion. | Golden external-boundary fixture gap | Dedicated output uses prompt-provided shot IDs and passes the production parser. | closed |
| BUG-GP-008 | Chapter render after three real video Tasks | The local media provider returned a 0.5-second MP4 with near-empty 44.1 kHz audio. The real 12-second loudness-normalization command produced NaN samples and FFmpeg rejected the chapter render. | Fail-before worker log contains the exact FFmpeg command and `Input contains (near) NaN/+-Inf`. Green rerun completed the real `chapter_render` Task in 1.782 seconds and persisted a 1920×1080, 12-second chapter output. The fixture also runs the same production loudnorm extraction as a construction-time contract. | Golden external-media fixture gap, not product behavior | The local provider now serves render-compatible 48 kHz stereo media through the real download, FFmpeg and storage chain. | closed |
| BUG-GP-009 | BGM score structured planning at `ready_to_generate_audio_layers` | The external-model simulator returned generic JSON instead of the production BGM score schema, so the real worker rejected the score before any music request. | Fail-before worker parser error. Green production-schema self-test validates the dedicated score response; a real rerun accepted the plan and advanced to FAL music submission. | Golden external-model fixture gap, not product behavior | Dedicated prompt routing derives the requested duration and returns the production BGM contract; no production parser or worker is replaced. | closed |
| BUG-GP-010 | FAL Lyria music submission after the real BGM plan | The local media simulator returned only `request_id`, while the production music adapter requires `request_id`, `status_url`, and `response_url`; the real Task failed `FAL_MUSIC_RESPONSE_URL_MISSING`. | Fail-before real worker/provider error. Green rerun executed production score parsing, FAL submission, status polling, audio download, 12.032-second transcode, storage and durable `music_score_plan` Task completion. The focused self-test also validates submission → status with `logs=0` → result including MIME type. | Golden external-media protocol fixture gap, not product behavior | The local server now implements the production three-endpoint queue contract. | closed |
| BUG-GP-011 | Soundscape structured planning after BGM completion | The generic local model response did not implement the production environment-bed schema or preserve the real timeline shot identities. | Production-schema self-test validates the dedicated response. Green real rerun executed `soundscape_plan`, parsed the actual three prompt shot IDs, persisted one source and one anchored section, and completed the durable Task. | Golden external-model fixture gap, not product behavior | Dedicated routing derives identities from the final rendered timeline while the production planner, parser and persistence remain real. | closed |
| BUG-TASK-001 | Concurrent image worker log context | Process-global task log context leaked one task identity into another concurrent image completion. | Fail-before mixed request/task evidence; a spawned real concurrency integration now proves isolated identities. | Worker observability context ownership | Node runtime uses real `AsyncLocalStorage`; missing capability fails explicitly instead of sharing mutable global context. | closed |
| BUG-TASK-002 | Real video provider returns HTTP 503 on the first submission | The provider explicitly declined the request, but the durable invocation fence converted every `FetchStatusError` to permanent `PROVIDER_SUBMISSION_REJECTED`; the worker therefore reported `retryable:false` and never used the Task definition's second attempt. | Fail-before real `GJ-WORKER-RETRY-RECOVERY` stopped at attempt one. Green: real-MySQL integration 7/7 proves same-attempt and concurrent newer-attempt fencing, permanent rejection and ambiguous outcome; the original Playwright variant then passed 7/7 with one `video_group` Task completed at attempt two, one Operation, reload survival and no failed duplicate. Terminal provider-failure variant remained 7/7 green. | Provider submission outcome and Task retry authority | The durable fence now records transient explicit non-acceptance separately and only a higher DB Task attempt can atomically reclaim submission. Permanent rejection remains closed; disconnect/timeout remains `outcome_unknown`; provider POST has no internal retry. | closed |
| BUG-TG-001 | `GJ-MODEL-STOPS-AFTER-CONFIRM` fault variant | The test required an “orchestrator” to continue after AI deliberately stopped without requesting the next operation. That expectation contradicted the product rule that AI-owned actions must be initiated by AI, and would reward a hard-coded server continuation. | Original real Journey failed with durable `workflow_continuation_missing`, no source document and no Task. User-confirmed architecture principle rejects deterministic execution of the missing AI action. | Test oracle defect, not product defect | The variant now requires explicit failure, unchanged workflow stage, one consumed Choice, and zero fabricated source documents/Tasks. It still detects silent success, partial commits, duplicate consumption and hard-coded continuation. | closed |
| BUG-TG-002 | Approval double-submit precondition reported `assistant_failure` while the current Approval card was visible | The generic boundary reader searched the whole visible message history and prioritized an old tool-failure card over the current server-owned pending Approval. The screenshot showed the real active Approval and button below the historical failure. | Fail-before timed out before clicking even though the screenshot and current workflow stage proved `ready_to_generate_videos` with a visible Approval. | Test oracle defect, not product defect | The double-submit scenario now asserts the stable current Approval control directly before and after reload. Historical messages remain evidence but no longer decide the current-interaction precondition. | closed |
| BUG-TG-003 | First task-disconnect variant observed no Task | The page was closed immediately after a button whose React handler intentionally launches the async request without making the DOM click await its settlement. Closing the page canceled the Approval request before any durable Task existed, so it did not exercise completion during disconnect. | Read-only oracle polled for 180 seconds and consistently found zero `video_group` Tasks; the failure occurred before the intended precondition. | Test injection boundary defect, not product defect | The scenario now waits for the real Approval HTTP response and a durable queued/processing Task before closing the page. It still closes before Task completion and uses only the DB oracle while disconnected. | closed |
| BUG-TG-004 | SSE stale-cursor replay failed a zero-duplicate assertion although all replay-owned counts were unchanged | The canonical Workflow Lab source already contained two duplicate Assistant tool-call identities from the separately owned lifecycle incident. The SSE scenario asserted global historical cleanliness instead of measuring whether replay introduced a new duplicate. | Before/after snapshots had identical Task, TaskEvent and Operation counts; failure listed two old tool-call IDs that predated the stale-cursor reload. | Test oracle scope defect, not an SSE product defect | Replay now compares duplicate message/tool identity sets before versus after the real stale-cursor reconnect. A changed set fails; inherited external-incident evidence remains visible without being attributed to SSE. | closed |
| BUG-TG-005 | Full checkpoint matrix reported early stages missing after the downstream staircase had used them successfully | The canonical manifest stored only one mutable `scope`. Every staircase fork replaced it with the newest clone, so later probes asked the last project in the lineage to list checkpoints that were created in earlier source projects. | Full matrix executed 19 cases: the staircase passed, then six probes failed immediately with `GOLDEN_STAGE_CHECKPOINT_MISSING` while checkpoints from later lineage positions remained available. | Golden checkpoint-source identity defect, not product behavior | The manifest now records `workflowStage → source project/episode` from the production Workflow Lab list at each real staircase position. Probes fork from the source that actually owns the checkpoint; no business row or stage is synthesized. | closed |
| BUG-TG-006 | Stage probes timed out with a valid pending Approval visible | The generic probe sampled the Approval button once before Session projection completed, then sent a composer message. It also forced the same tool while resuming an already frozen Approval, causing a second invocation, and treated every workflow enum as a reusable checkpoint even when the state was only an internal transient between AI actions. | Matrix red: asset/video probes stayed `awaiting_approval`; video then showed a completed approved invocation followed by a duplicate operation conflict. `ready_to_generate_edit_script` never had a checkpoint and `ready_to_render_final` was already completed when reconstructed from history. | Test driver and admission-oracle defect | Probe behavior now follows checkpoint kind: Choice/UI boundary, Approval reload/consume, or AI user turn. A newly raised Approval is consumed as a second real boundary; forced tools are cleared before Approval resume. Only 11 durable/reusable stages are admitted; transient states remain covered inside the full mainline. | closed |
| BUG-CHOICE-001 | task completion at `bible_ready_for_review` | AI correctly initiated `request_edit_bible_review_choice`, but the card builder compared the durable EditBible status to the workflow stage string `bible_ready_for_review` and rejected the legal durable value `ready_for_review`. The Run became `PROJECT_AGENT_TOOL_ERROR` although the task and bundle were complete. | Dynamic-tool output: `EDIT_FIRST_BIBLE_REVIEW_NOT_READY:ready_for_review`; read-only oracle shows completed task, durable bible `ready_for_review`, projected workflow `bible_ready_for_review`, then failed Run | Cross-layer status vocabulary mismatch | The formal Assistant repair compares the canonical durable entity status. No discovery-only code remains; the original Journey must revalidate it. | repair integrated; revalidation pending |

## Required evidence for BUG-GH-001 closure

- Two distinct Golden/test service scopes can start without sharing Compose
  project names, container names, host ports, databases, Redis instances, or
  artifact environment identity.
- Stopping one scope leaves the other scope and development services running.
- Startup failure cleans up only the failed scope.
- The Golden environment writes the actual scoped database/Redis endpoints to
  its descriptor and the read-only oracle connects to that database.
- `npm run test:golden:self` remains green and one real browser scenario reaches
  the application after the isolation change.

## Resolved local infrastructure incident

On 2026-07-12 Docker Desktop kept existing all-interface published ports
(`3307`, `13306`) reachable but newly published all-interface Compose ports
accepted TCP and then timed out before the MySQL protocol reached the
container. Container-local MySQL and Redis health checks remained green and
MySQL `SHOW PROCESSLIST` saw no host probe. Two bounded `test:golden:product`
attempts therefore failed before Playwright test collection began: five
product scenarios were **not run**, not failed. A disposable control container
then proved that a newly published dynamic Redis port bound explicitly to
`127.0.0.1` returned a real `PONG`. The scoped test Compose now binds its
dynamic MySQL and Redis ports only to loopback. Repeated fully started
Playwright runs proved the protocol path and scoped teardown without restarting
Docker Desktop or touching shared services.
