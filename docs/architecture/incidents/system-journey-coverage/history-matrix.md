# System Journey coverage historical regression matrix

| Symptom | Root cause | Previous defense | Why it escaped | Required defense |
| --- | --- | --- | --- | --- |
| Thousands of isolated tests passed while the real Assistant composition failed | Tests removed the UI/runtime/DB/queue/worker/SSE handoffs where ownership conflicted | Unit, mocked integration, route catalogs, mutation and coverage machinery | Local assertions reproduced current implementation instead of an independent user result | Real browser journeys with durable read-only oracle and controlled external boundaries |
| The first Golden system found real bugs but represented only one product family | Golden was created as an Assistant incident diagnostic rather than a product-wide journey map | 23 Assistant mainline/stage/fault scenarios | Scenario count and exhaustive Assistant stages looked broad while project, permission, billing, locale, and deployment results had no browser evidence | Product journey/risk map based on independent user results, not route or stage count |
| Golden environment startup removed another environment's containers | Shared test helper used the checkout's implicit Compose project, fixed container names/ports, and global `down -v --remove-orphans` | Harness self-tests covered providers, network policy, scenario mount, and oracle write denial | No self-test started two independent infrastructure owners or checked stop isolation | Run-scoped Compose identity, dynamic endpoints, scoped cleanup, and concurrent isolation self-test |
| A second Golden run accepted stale readiness and mixed a new database with an old app/worker | App, coordinator and Redis endpoints were not part of the scoped runtime identity; teardown removed data before the process tree stopped | `reuseExistingServer: false` and signal handlers on the TypeScript launcher | A fixed health port could answer before the new launcher finished, and Playwright terminated the wrapper rather than the real owner | One inherited runtime identity for app/coordinator/services, authenticated coordinated shutdown, process-group termination, bounded readiness and predeclared-scope cleanup |
| A harness startup failure left containers alive | The scoped endpoints variable was assigned only after readiness completed, so catch had no returned scope to clean | Catch block stopped services only when `prepareFreshTestServices` returned | The failing state existed between Compose start and endpoint return, outside the cleanup condition | Record scope before any side effect and clean it unconditionally on startup failure; prove no scoped containers survive |
| Assistant lifecycle failures hid unrelated authentication, project and permission results | All browser scenarios shared the Assistant story-to-video prerequisite | One longest mainline plus checkpoint probes | A red early stage made the test report look globally blocked even though independent product families were never attempted | Separate product-journey contracts and command for auth/session, project CRUD, cross-user ownership, locale identity and deployment capabilities |
| Browser boundary detection used translated copy as lifecycle authority | Diagnostic page objects optimized for explaining the first incident | Durable oracle existed, but copy checks selected the next action | Chinese copy happened to match the initial UI and did not challenge locale or redesign | Stable `data-testid`/identity for user actions plus durable stage/run/task assertions; copy only proves comprehension |
| An early mainline blocker prevented downstream product discovery | Stage probes depended on checkpoints produced by the same mainline, but the discovery workflow did not formally separate blockage from later coverage | Workflow Lab fork helper and declared checkpoint stages | Missing source checkpoints stopped probes, encouraging repeated work on the earliest failure | Canonical source checkpoint lifecycle, explicit missing-checkpoint evidence, and discovery loop that resumes downstream before permanent repair |
| A Workflow Lab checkpoint projected the requested stage but could not execute its next legal action | Clone logic copied the headline Bible row while dropping chapter plans and inferred all later ownership as chapter zero | Route/service tests asserted that a fork returned and had a stage-shaped payload | They did not execute the next production operation from the cloned state, so missing transitive domain facts stayed invisible | Every canonical checkpoint must execute its next real UI boundary; clone all canonical identities and fail on unmapped ownership instead of substituting the default chapter |
| Workflow Lab listed an Approval that POST could never restore, or a restored Approval disappeared from the next list | Message/tool-call history was treated as interruption authority; duplicate tool IDs overwrote each other, missing runState was not part of admission, and pending durable interruptions without a message part were invisible | Earlier tests proved only first-generation Choice/Approval projection | They did not require list → fork → list → fork → consume, so one-shot checkpoints looked reusable | Durable interruptionId is primary; tool fallback only when unique; Approval listing requires runState; pending durable boundaries are listed directly and repeated fork/consume is a real Journey |
| A historical checkpoint copied facts from the source project's later completed state | The checkpoint stored a message position but domain clone queries read the source's current rows; the harness also overwrote earliest stage ownership with the latest mutable scope | Stage-shaped clone normalization and a single mutable fixture manifest | A correct stage label hid Bible JSON, asset targets, video groups and final outputs from a later time | Preserve/freeze the earliest real source per reusable stage; do not admit internal transient enums as checkpoints; execute the next real action from every retained source |
| A second Approval fork collided on planned IDs or failed its stale-value fence | Planned identities were implicit inside operation-specific metadata and asset clone normalization cleared an association that the frozen plan expected | Project/episode/artifact ID maps and plan snapshot hash rewriting | Task target IDs were only part of the identity graph; parent IDs reserved for commit and prior association values were outside the map | `OperationPlan.reservedIdentityIds` explicitly enumerates plan-time identities; one replacement map rewrites plan, quote, payload and runState; clone preserves mapped pre-approval associations |
| An authenticated user used their own project to overwrite another user's project asset | Route auth proved only the request's project; the asset service treated an unrelated target ID as ownership and used unscoped mutation lookups | Project route auth and a source-global `userId` filter | No test composed a valid authenticated project with a foreign target ID; route tests repeated the supplied IDs | One service-level scope resolver verifies owner/project/kind/asset/variant; copy validates both ends atomically; real two-user Chromium Journey preserves the foreign target |
| A successful project mutation logged a false fetch failure during its next navigation | The page launched a refetch without owning/awaiting its lifetime; create did this before leaving the page, while delete repeated the same invariant when a user navigated immediately afterward | The create branch was fixed after its own Journey, but the sibling delete branch retained its 2026 initial implementation | Tests ended after deletion and never composed delete with an immediate second user action | Every action chooses one successor; a stay-on-page mutation awaits its refresh, while a navigation successor starts no page-local refresh |
| Asset Hub initially returned 500 but appeared after client recovery | A client-marked SSE hook read browser storage during server render | Cursor parser and SSE sequence unit tests | They called pure protocol functions and never requested the real server-rendered page | Real first-document Journey plus one capability-gated cursor reader: server starts empty, browser restores durable cursor |
| Logout returned 200 while the same page still displayed an authenticated user | A hand-written NextAuth POST changed the cookie but not SessionProvider's client cache | Route status/session endpoint checks | Server session and browser session were observed separately, so two writers could disagree | One NextAuth client signout owner and a Journey that simultaneously checks Navbar, session endpoint and durable user identity |
| A foreign project 403 was followed by an episode-create POST | Zero episodes was interpreted without first proving a Project had loaded successfully | API routes independently rejected both requests | Route-level 403 tests could not prove the client stopped after the first denied authority read | Direct foreign URL Journey; all derived setup writes require a successful authoritative parent resource |
| A real provider HTTP 503 never reached the configured second worker attempt | The durable provider fence classified every explicit non-2xx submission response as permanent rejection, overriding the Task retry policy | Unified Task retry classes plus at-most-once provider checkpoint replay tests | Tests covered accepted replay, concurrent claims and ambiguous disconnect, but never an explicit transient non-acceptance followed by a newer durable Task attempt | One durable submission outcome authority: transient explicit non-acceptance may be atomically reclaimed only by a newer Task attempt; permanent rejection and ambiguous outcomes remain non-retryable; verify with real MySQL and the original browser/queue/worker Journey |
