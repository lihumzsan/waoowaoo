# Historical test-governance regression matrix

| Symptom | Root cause | Previous defense | Why it escaped | Reset defense |
| --- | --- | --- | --- | --- |
| Hundreds of tests passed while the real Assistant workflow failed | Internal layers were isolated at the exact handoff where lifecycle facts conflicted | Unit and mocked integration suites | The failing composition did not exist in the test process | Browser Golden Journey with real UI, runtime, DB, queues, workers, SSE, and reload |
| Route coverage appeared exhaustive | Every route was invoked behind mocked authentication and mocked internal dependencies | Route catalog, behavior ledger, route conformance | A rejection `Response` counted as execution without proving route semantics | Real user journeys; structural route rules remain checks rather than product proof |
| Historical regressions appeared to have fail-before evidence | Tests supplied a fabricated faulty return value directly to their own assertion | Historical catalog and semantic-fault registry | The historical production path was not executed | Pre-fix revision or a controlled fault at the real entry boundary |
| Test count continued to grow | Repository rules required a test for every modification and named a directory for each code category | Changed-file guard, test-size guard, requirements matrix, mandatory suite list | Compliance measured files and mounts rather than fault detection | Admission contract based on independent oracle and falsification |
| Mutation score suggested strength without product protection | Mutations targeted a large unit-test corpus detached from real composition | Stryker baseline and incremental mutation gate | Killing local mutations did not exercise cross-process handoffs | Targeted red evidence for pure logic and real historical defects |
| Real browser diagnostics existed outside the normal completeness definition | `verify:push` aggregated legacy Vitest suites and did not own Golden Journey | `test:all`, required-suite verifier | The proven high-signal layer was additive rather than authoritative | Golden Journey is the sole browser-complete product evidence; scheduling is deferred |
