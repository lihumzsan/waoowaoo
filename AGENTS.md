# AGENTS.md

## 适用范围
- 本规范适用于本仓库的所有目录与文件。
- 若下级目录存在新的 `AGENTS.md`，下级规范仅可补充，不可弱化本文件的强约束。

## 项目目标与编码原则
- 项目定位为全新系统，`统一` 与 `简洁` 是最高优先级。
- 禁止以“兼容旧代码/旧行为”为理由引入冗余分支、兼容层、双轨逻辑或临时补丁。
- 新功能与重构应优先服务于一致性、可维护性、可读性，而非历史包袱。
- 禁止使用任何any类型，必须明确类型
- 禁止硬编码固定语言，按照项目规范做i18n

## 文件与模块化要求
- 大文件必须拆分为清晰模块，按职责边界组织。
- 单个文件若同时承担多类职责（如 UI、状态、数据请求、转换逻辑混杂）必须拆分。
- 公共能力应抽离为可复用模块，避免复制粘贴。
- 命名必须体现职责，目录结构应支持快速定位与阅读。

## 仓库架构硬约束
- route 只负责鉴权、参数校验、提交任务与返回响应；复杂业务一律下沉到 `src/lib/**`，禁止在 route 内堆业务逻辑。
- 所有 route 必须复用统一鉴权与错误出口：`apiHandler`、`requireUserAuth`、`requireProjectAuth`、`requireProjectAuthLight`；禁止手写分散鉴权和裸错误响应。
- 涉及生成/修改/重生成图片的输入，必须走 `src/lib/media/outbound-image.ts` 统一归一化；禁止绕过该链路直接传原始 URL、`/_next/image`、storage key 或 base64。
- 前端浮层（modal/popover/dropdown/tooltip/教程高亮）默认通过 portal 挂到 `document.body`；禁止挂在 card、列表项、`overflow-hidden` 或会创建层叠上下文的容器内。
- payload、route、worker 的字段语义必须共用同一份类型或归一化函数；禁止前端允许、worker 拒绝，或 A 端写入、B 端按另一语义读取。
- 涉及“创建记录后再提交任务”的流程，必须先完成前置校验；若任务提交可能失败，必须有显式补偿/回滚，禁止留下僵尸数据。
- 页面导航必须使用 `@/i18n/navigation`；禁止硬编码根字面量页面路由。
- 云端/开源差异只能通过 `src/lib/deployment/config.ts` 与 `src/lib/deployment/features.ts` 派生；`DEPLOYMENT_EDITION=cloud|self-hosted` 是唯一版本来源，禁止用 `NODE_ENV`、域名、路由、`NEXT_PUBLIC_*`、文件存在性等另建区分方式。

## 既有架构优先
- 新增功能或修复问题前，必须先检索并理解仓库内已有的同类实现、权威入口、共享类型和公共模块。
- 未证明现有架构无法承载前，禁止另起一套并行实现，包括新的入口、状态来源、协议语义、数据流或局部私有逻辑。
- 如果已有公共模块能力不足，必须优先补全原模块，并收敛旧的分散逻辑；禁止在调用处临时手写一份。
- 新增架构能力必须放在清晰的共享边界内，并说明为什么现有入口不能承载。
- 交付说明中应说明复用了或补全了哪个既有模块；如果没有复用，必须说明原因；若属于同类实例新增，必须附带本节要求的对齐表。

## 新增同类实例必须对齐权威参照物
- 当新增内容属于既有架构中的同类实例时，包括但不限于新的节点类型、任务类型、provider 模态、生成阶段、队列消费者、worker 链路、Canvas 节点、异步状态机分支、音频层或媒体层，禁止凭经验实现，必须先选择一个最接近的既有权威同类作为参照物。
- 步骤 1：指明参照物。必须说明选择了哪个既有实现作为 reference implementation，以及为什么它是最接近的新实例参照物。例如新增 Soundscape 节点时，参照物应是 BGM 节点。
- 步骤 2：枚举参照物触点。必须用 `rg` 检索参照物的任务类型、节点类型、模态 key、runtime target、DB 模型、operationId、状态字段、i18n key 等实际标识符，列出它在所有层参与的触点。至少覆盖 route、queue、worker、DB model、计费 apiType、状态机边沿、structured stream adapter、Canvas 节点、展开态、二级菜单、focus-follow、operation-result、防旧状态覆盖、i18n、权限、测试。该列表只是最低提醒项，权威清单以参照物实际 grep 命中的触点为准，禁止把提醒项当作完整 checklist。
- 步骤 3：逐条对齐。新实例必须对参照物触点清单中的每一项给出等价实现，或明确写出“不适用”及原因。禁止默认跳过、漏列、只实现核心路径后收工。
- 步骤 4：处理差异。若新实例比参照物少接任何一层，尤其是流式传输、展开态、状态生命周期、防旧状态覆盖、失败边沿或重试入口，必须视为架构不一致缺陷，补齐或明确记录迁移条件与删除时机；禁止以“核心功能已通”为理由保留局部实现。
- 步骤 5：交付对齐表。最终交付必须包含 `参照物触点 -> 新实例覆盖情况 / 不适用原因` 的对齐表。没有这张表，不算完成同类实例新增。
- 判据：`我实现了我以为需要的` 属于局部视野，不合格；`我枚举了参照物的全部触点并逐条对齐` 才算合格。

## 数据安全与高风险操作
- 任何可能导致数据 `删除`、`丢失`、`覆盖`、`结构变更`、`不可逆修改` 的操作，执行前必须获得用户明确同意。
- 未获得明确同意时，仅允许进行只读分析、方案设计与风险说明，不得落地执行。
- 涉及数据库、文件批量改写、迁移脚本、清理脚本、覆盖写入等场景，一律按高风险处理。
- 云端私有资料不得进入开源版本：`.env*`（example 除外）、`.env.cloud.local`、`.official-content/**`、证书/私钥/生产部署文件、平台或支付密钥、商户/公司注册/地址/董事/UBO/KYC/银行/支付审核资料只能保留在忽略文件或云端私有配置中；开源代码、messages、README、example 只能放占位或通用文案。
- 但可以运行测试、构建等无害的操作。
- 可以执行测试，构建等没有毁灭性的命令


## 思维与决策方法
- 所有方案必须采用第一性原理：先明确目标、约束与事实，再推导实现路径。
- 禁止基于“惯例如此”或“历史如此”直接做决策；必须说明核心假设与取舍依据。
- 实现应追求最小必要复杂度，避免无效抽象与过度设计。
- 遇到 bug、状态不一致、异步竞态、跨模块语义不一致时，必须先定位 `权威事实来源`、`状态生命周期`、`职责边界` 和 `失败边界`，再提出修复；禁止在未证明根因前直接堆条件分支。
- 若同一问题经历一次修复后仍复现，必须暂停补丁式修改，重新做根因分析并说明上一版修复为什么没有覆盖真实路径；禁止继续在原有启发式上叠加新的例外条件。
- 方案评估必须回答：要删除或收敛哪一条旧逻辑、是否减少状态来源、是否消除双轨、是否让错误更显式；若答案是否定，默认不是合格的架构修复。

## 架构修复与反补丁要求
- 修复应优先收敛到单一权威状态、单一类型语义、单一执行入口或单一生命周期；禁止新增与旧链路并行的第二套状态、第二套解析、第二套兼容分支。
- 禁止用“再加一个 if / 再兜底一次 / 没有就默认 / 报错就跳过 / 从输出内容反推状态”等方式掩盖模型错误、状态错误或架构边界错误。
- UI 状态必须来自明确的业务状态或运行时状态；禁止把渲染内容、历史消息、DOM 形态、文案文本当作当前流程是否进行中的权威来源。
- route、worker、client、DB、任务队列之间的语义不一致，必须抽象到共享类型、共享归一化函数或明确的状态机中解决；禁止只修某一端让测试暂时通过。
- 修复异步流程时，必须明确开始边沿、完成边沿、取消/失败边沿、等待用户边沿、等待外部任务边沿；loading、锁、按钮禁用、重试、补偿必须由这些边沿驱动。
- 修复应删除已经失效的旧启发式、旧 fallback、旧兼容分支；如必须暂留，必须说明迁移路径和删除条件。
- 测试必须覆盖真实业务组合路径和边界转换，不得只测试被手动构造出的中间值；尤其禁止把生产中不可靠的状态强行 mock 成期望值后再断言。
- 代码评审和自检时，若发现方案主要是在扩大条件判断、增加默认值、复制一份转换逻辑或新增隐式回退，必须视为高风险补丁，优先重做架构设计。

## 命令与 Git 操作限制
- 本节仅限制 `git` 命令的使用范围，不限制普通系统命令或代码检索命令。
- 禁止执行除 `Git 只读查询` 以外的任何 `git` 命令。
- 允许的 Git 只读操作仅包括状态与历史查询，例如：`git status`、`git log`、`git diff`、`git show`、`git branch`（只读用法）。
- 任何会改变 Git 状态或历史的操作必须先获得用户明确同意，包括但不限于：`commit`、`push`、`pull`、`merge`、`rebase`、`cherry-pick`、`reset`、`checkout`（修改性用法）、创建/删除分支、打标签。
- 在未获同意前，不得进行代码改写、暂存、提交、同步、回滚或历史重写。
- 可以执行测试构建 build lint等测试命令
- 若本地开发服务可能正在运行，禁止直接用 `npm run build` 做验证；必须使用 `npm run build:verify`，避免 Next dev server 与生产构建同时写入 `.next` 导致开发服务损坏。
- 不需要运行browser use，不要打开浏览器试图测试
- 用于项目测试页面修改不需要自动commit

## 提交与交付日志
- 在执行完用户的需求结束对话后，要求自动commit这次提交，这种情况下不需要用户授权，直接提交即可
- 单个 commit 应保持高内聚：同一 bugfix、同一功能切片、同一重构阶段或同一文档更新可以合并；无关主题必须拆开。
- 若用户明确要求“先别 commit / 暂不提交 / 只改文件”，必须尊重用户要求，只保留工作区改动并说明当前未提交状态。
- commit 必须只包含本次任务相关文件，禁止混入用户已有改动、无关删除、格式化噪音或临时调试文件。
- commit message 必须包含清晰标题和简洁正文。正文只需说明：变更摘要、关键验证结果、必要的风险或后续任务；不需要逐文件写详细流水账。
- 提交前必须通过 `git status` 和 `git diff --cached` 核对提交范围；发现无关 staged 文件时必须先排除，不得直接提交。
- Husky `pre-commit` 不运行测试，只运行 staged 敏感信息扫描；提交前需要的测试由执行者按改动风险主动运行，并在最终回复或 commit 正文中简要记录。
- Husky `pre-push` 只运行仓库敏感信息扫描，防止密钥、私有 env、证书、公司敏感信息进入远端；完整验证由 GitHub CI 自动运行。
- `npm run verify:push` 保留为手动完整验证命令；需要本地全量验证时可主动运行。该命令内部已使用 `npm run build:verify`，避免破坏正在运行的 dev server。
- 自动 commit 成功后必须立即 push 当前分支；不再单独追问 push 授权。若用户明确要求“先别 push / 暂不 push / 只本地 commit / 只改文件”，必须尊重该要求并说明未推送状态。
- 用户要求的临时功能测试页面修改时不需要提交

## 不掩盖任何问题
- 不要做任何不必要的回退逻辑，特别是有可能隐藏问题的，除非用户允许，否则禁止做，如发现一个模型不可用时自动跳转到新的模型，或代码失效时，报错时直接略过，或者没有的时候提供默认值等错误操作，或制造假数据等。
-系统执行必须遵循显式失败与零隐式回退原则：严禁静默跳过错误、隐式配置兜底或自动模型降级，确保所有非预期行为原地崩溃并如实上报。


## 敢于合理质疑用户 了解用户真实需求
- 提问以了解我真正需要什么（不仅仅是我说什么）。
- 用户可能不够了解代码 对技术的理解可能不如你 
- 用户和你说的作为参考 而不是绝对值 如果某些事情说不通，请挑战我的假设。
- 在模糊以及猜测出用户可能不了解架构代码时，你可以多提问用户问题，揣测用户真正的意思

## 测试规范
- 新增功能或修改功能逻辑必须进行测试，新增功能必须增加测试，如修改文件导致需修改测试文件务必一起进行修改，确保测试百分百跟随
- 改 worker 逻辑 / 修 bug / 加 route 或 task type → 必须写或更新测试
- 改 provider / gateway / async poll / generator 协议 → 必须写或更新 `tests/integration/provider/**`
- 改 dedupe / enqueue / rollback / orphan recovery → 必须写或更新 `tests/integration/task/**` 或 `tests/regression/**`
- 改 P0 链路且影响 route -> task -> worker -> DB 语义 → 必须写或更新 `tests/system/**`
- 修 bug 必须同步新增回归测试，`it()` 名称体现该 bug 场景
- 断言必须检查具体值（DB 写入字段值、函数入参、返回值），禁止只用 `toHaveBeenCalled()`
- 禁止"自给自答"：mock 返回 X 再断言 X，没有经过任何业务逻辑

### 测试结构简规

- 测试目录必须按 `unit / integration / system / regression / contracts` 分层，禁止跨层混放。
- `tests/integration/api/contract` 只放 route 契约；`tests/integration/api/specific` 只放具体业务场景。
- 同一 stage/runtime 的碎片测试应优先合并；超过约 `350` 行或 `10` 个 `it()` 的测试文件必须拆分。
- 命名统一使用：`*.route.test.ts`、`*-routes.test.ts`、`*-<behavior>.test.ts`、`*.integration.test.ts`、`*.system.test.ts`。
- 新增或改动测试时，优先复用现有 helpers/fixtures，禁止同主题重复造 mock 框架。
